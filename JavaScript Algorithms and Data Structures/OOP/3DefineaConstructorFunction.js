/**
 * they define properties and behaviors that will belong to
 * the new object.
 * think of them as a blueprint for the creations of new object
 */
 function Dog(){
    this.name = "GukGuk";
    this.color = "Red";
    this.numLegs = 4;
  }