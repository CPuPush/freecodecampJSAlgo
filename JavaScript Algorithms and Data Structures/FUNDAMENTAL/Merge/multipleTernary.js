function checkSign(num) {
    return (num == 0)? "zero"
    : (num > 0) ? "positive"
    : "negative";
}
console.log(checkSign(-1));

//how to user multiple conditional with ternary
/**
 * return (if nya) ? "Hasilnya apa"
 * : (else if nya) ? "hasilnya"
 * : "else string akhir"
 */