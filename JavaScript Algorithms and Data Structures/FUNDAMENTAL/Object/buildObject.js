const myDog = {
    // Only change code below this line
    name: "budost",
    legs: 4,
    tails: "long",
    friend: ["cat", "bird"]
    // Only change code above this line
};

/**
 * object are similare with array, but using property to call data.
 * const cat = {
  "name": "Whiskers",
  "legs": 4,
  "tails": 1,
  "enemies": ["Water", "Dogs"]
};
->
if your object has any non-string properties, JavaScript will automatically typecast them as strings.


 */