// Setup
function testEqual(val) {
    if (val == 12) { // Change this line
      return "Equal";
    }
    return "Not Equal";
  }
  
  testEqual(12);
//equality is ==
//assignment is =
//there are different
//in order for JS to compare two different data typer(strings and numbers)
// it must convert one type to another, this is known as type coersion. (tipe pemaksaan)
/**
 * 12 == "12"
 * 12 !=== "12"
 */