/**
 * parameter are variable that art as placeholders for the values that are to be input to a function when it is called(when create function)
 * argument is the actual value that are input  into a function(when call function)
 */
function functionWithArgs(one, two){
    console.log(one+two);
    
}
functionWithArgs(1,2)
