/**
 * splice can take up to 3 parameters.
 * 1. first parameter represents the index on the array from which to begin removing elements
 * 2. second parameter indicates the number of elements to delete
 * not only delete but also returns a new array containing the value of the removed elements
 * 
 */

 const arr = [2, 4, 5, 1, 7, 5, 2, 1];
 // Only change code below this line
   //  arr.splice(0,1);
    arr.splice(3,1)
 // Only change code above this line
 console.log(dark);