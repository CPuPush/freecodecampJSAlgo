/**
 * the parseInt() function parses a string and returns an integer
 */
 function convertToInteger(str) {
    const numString = parseInt(str);
    return numString;
}

console.log(convertToInteger("000056"));
